import styled from 'styled-components'

export const StyledControls = styled.div`
    position: absolute;
    z-index: 10;
    top: 100px;
    left: 50%;
    transform: translateX(-50%);

    label, select, option {
        font-size:25px;
    }
`